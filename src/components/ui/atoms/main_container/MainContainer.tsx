import styled from 'styled-components';

const MainContainer = styled.div`
  display: flex;
  height: 100%;
  pointer-events: all;
  position: relative;

  background: white;
`;

export default MainContainer;
