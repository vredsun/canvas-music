import styled from 'styled-components';

const UiAppContainer = styled.div`
  height: 100%;
  pointer-events: all;

  background: white;
`;

const UI = {
  AppContainer: UiAppContainer,
};

export default UI;
